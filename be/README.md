# Home Inventory — Backend

Бэкенд для приложения домашней инвентаризации. Учёт вещей, локаций, документов и гарантий.

## Стек

| Слой               | Технология                                       |
| ------------------ | ------------------------------------------------ |
| Framework          | NestJS 11 + TypeScript                           |
| База данных        | MongoDB + Mongoose                               |
| Кэш                | Redis + ioredis                                  |
| Очереди            | BullMQ                                           |
| Файловое хранилище | S3 (AWS SDK v3) / MinIO                          |
| Почта              | Nodemailer + @nestjs-modules/mailer + Handlebars |
| Аутентификация     | JWT + Passport                                   |
| AI                 | openai sdk                                       |

### Структура

```
src/
├── api/          # фичевые модули (controllers + services + dto)
├── config/       # фабрики конфигов для DynamicModule.forRootAsync
├── infra/        # технические модули (database, redis)
├── libs/         # внутренние сервисы-обёртки (storage, mail)
├── shared/       # типы, утилиты, константы
├── app.module.ts
└── main.ts
```

### Локальная инфра (docker-compose)

| Сервис    | Порты                  | Назначение                 |
| --------- | ---------------------- | -------------------------- |
| `mongo`   | 27017                  | Основная БД                |
| `redis`   | 6379                   | Кэш                        |
| `minio`   | 9000 (API), 9001 (UI)  | S3-совместимое хранилище   |
| `mailpit` | 1025 (SMTP), 8025 (UI) | Локальный SMTP-перехватчик |

### Запуск

```bash
cp .env.example .env
docker compose up -d
npm install
npm run start:dev
```

UI-консоли:

- Swagger → http://localhost:3000/swagger
- BullMQ → http://localhost:3000/queues
- MinIO → http://localhost:9001 (minioadmin / minioadmin), создать bucket `home-inventory`
- Mailpit → http://localhost:8025 (все письма из приложения ловятся сюда)

---

## Функциональные требования

### Аутентификация

- Регистрация по email + пароль
- Вход по email + пароль
- JWT access token + refresh token в httpOnly cookie
- Обновление access token по refresh token
- Выход (инвалидация refresh токена)

---

### Вещи (Item)

- CRUD
- Фильтрация по категории и контейнеру
- Полнотекстовый поиск по названию и описанию
- Загрузка фото через presigned S3 URL
- Генерация QR-кода при создании
- Произвольные пользовательские поля (custom fields): до 20 типизированных пар ключ/значение (string / number / date / boolean)

---

### Контейнеры

- CRUD
- Иерархическая структура с типами (комната → шкаф → ящик → коробка → сумка)
- Правила вложения (ContainerRule): какой тип во что можно класть; правило копируется от родителя при создании

---

### Категории

- CRUD
- Плоский список (без иерархии)
- Имена уникальные; базовый набор засевается при старте приложения

---

### Документы

- Прикрепить файл к вещи
- Тип документа (receipt / warranty / manual / other)
- Дата окончания гарантии

---

### Напоминания

- Cron-job раз в день — проверка истекающих гарантий
- Email уведомление за 30, 7 и 1 день до истечения
- Дедупликация повторных отправок

---

### Фоновые задачи (BullMQ)

- Генерация QR-кода после создания вещи
- Сжатие фото после загрузки (in-place, формат сохраняется)
- Генерация PDF-отчёта по контейнеру

---

### Кэш (Redis)

- Кэширование списка вещей
- Инвалидация при изменении данных
- Кэширование деревьев контейнеров и категорий

---

### Экспорт

- PDF-отчёт по контейнеру — список вещей с фото и custom fields
- Асинхронная генерация (BullMQ) — клиент получает id отчёта и поллит статус
- Готовые отчёты живут 7 дней, потом чистятся cron-задачей
- SSE-уведомление о готовности отчёта

---

### Распознавание по фото (AI)

- Кадр с камеры → vision-модель распознаёт вещь и предлагает черновик формы
  (название, описание, категория, custom fields)
- Распознавание **не создаёт** вещь — отдаёт черновик, юзер правит и сабмитит `POST /items`
- Асинхронно (BullMQ): клиент получает id, поллит статус + SSE-уведомление о готовности распознавания
- Отмена запущенного распознавания (`DELETE /recognitions/:id`)
